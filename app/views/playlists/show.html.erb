<p>
  <h1><%= @playlist.name %></h1>
</p>

  <h2> Does the user follow me? <%= @playlist.does_user_follow?(current_user) %></h2>

<p>
  <strong>Description:</strong>
  <%= @playlist.description %>
</p>

<p>
  <strong>Author:</strong>
  <%= @playlist.original_author_dj_name %>
</p>

<% if @songs.length > 0 %>

<h2>Songs</h2>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Artist</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.name %></td>
        <td><%= song.artist %></td>
        <td><%= link_to 'Show', "/playlists/#{@playlist.id}/songs/#{song.id}" %></td>
        <td><%= link_to 'Edit', "/playlists/#{@playlist.id}/songs/#{song.id}/edit" %></td>
        <td><%= link_to 'Destroy', "/playlists/#{@playlist.id}/songs/#{song.id}/destroy", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% else %>

<h2>This playlist has no songs on it. </h2>

<% end %>

<%= link_to 'Add Song to Playlist', new_song_path(pid: @playlist.id) %> |
<%= link_to 'Edit Playlist Properties', edit_playlist_path(pid: @playlist.id) %> |
<%= link_to 'Back', playlists_path %>
