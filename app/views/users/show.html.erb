<p id="notice"><%= notice %></p>

<p>
  <strong>Dj name:</strong>
  <%= @user.dj_name %>
</p>

<% if current_user.id == @user.id %>
<%= button_to 'Delete Account', user_path(@user), method: :delete, data:{confirm: 'Are you sure you would like 
to delete this account?'} %>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
